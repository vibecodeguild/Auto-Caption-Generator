# Fonts

Place optional `.ttf` or `.otf` font files in this folder.

The app can reference installed system fonts by family name, but bundling font
files here improves FFmpeg subtitle rendering consistency across machines.
Only include fonts that you have permission to redistribute in a public repo.

Montserrat is bundled under the SIL Open Font License so the default caption
style renders consistently in FFmpeg.
